{
  "buildCommand": "cd ../.. && npm install && cd packages/db && npx prisma generate && cd ../../apps/web && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "cd ../.. && npm install"
}

